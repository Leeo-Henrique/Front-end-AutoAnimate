import styled, { createGlobalStyle, css } from "styled-components";

export const GlobalStyle = createGlobalStyle`
  :root{
    --mainColor : #deceff;
    --alternativeCollor: #ece2ff;
    --buttonColor : #4711de;
    --fontColor: #484444;
    --closeButton: #ff0000;
    --purple-md: #350b5a;
  }
`;
